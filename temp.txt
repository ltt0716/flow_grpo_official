# ============ 复制运行区(在训练机上) ============
# 重新解析 wandb reward(修了取列名的 bug)
cd /opt/project/p/codes/flow_grpo_official
git pull
python debug/extract_wandb_reward.py

# 把结果推给我看(csv + 脚本打印的样例记录)
git add -f debug/wandb_reward.csv
git commit -m "reward csv v2"
git push
# =================================================
